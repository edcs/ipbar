.build/
dist/

# Generated deterministically by Tools/GenerateIcon.swift — the script is the
# source of truth, so the rendered output stays out of diffs.
Resources/AppIcon.iconset/
Resources/AppIcon.icns
.swiftpm/
*.xcodeproj
*.xcworkspace
.DS_Store

# Visual brainstorming mockups — scratch, not part of the build.
.superpowers/

# The global gitignore excludes "docs" and "docs/*" everywhere.
# This repo keeps its design specs, so both rules are undone here.
!docs/
!docs/*
